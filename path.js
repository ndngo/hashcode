class Path {
  // startIntersection is an integer
  // endIntersection is an integer
  // name is a string
  // Length is an integer
constructor(numOfStreets, streetNames) {
    this.numOfStreets = numOfStreets;
    this.streetNames = streetNames;
  }
}

module.exports.Path = Path;
